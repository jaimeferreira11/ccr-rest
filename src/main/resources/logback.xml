<?xml version="1.0" encoding="UTF-8"?>
<configuration>
	<property name="logPattern" value="%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level %logger{35} - %msg%n" />
	<property name="logEncoding" value="UTF-8" />
	<property name="logDirectory" value="log" />

	<appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
		<layout class="ch.qos.logback.classic.PatternLayout">
			<Pattern>%d{HH:mm:ss.SSS} [%t] %-5level %logger{36} - %msg%n</Pattern>
		</layout>
	</appender>

	<appender name="fileAppender"
		class="ch.qos.logback.core.rolling.RollingFileAppender">
		<file>${logDirectory}/ccr-api-rest.log</file>
		<rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
			<fileNamePattern>${logDirectory}/ccr-api-rest_%d{yyyy-MM-dd}.%i.log</fileNamePattern>
			<timeBasedFileNamingAndTriggeringPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
				<maxFileSize>30MB</maxFileSize>
			</timeBasedFileNamingAndTriggeringPolicy>
			<maxHistory>30</maxHistory>
			<totalSizeCap>500MB</totalSizeCap>
		</rollingPolicy>
		<encoder>
			<charset>${logEncoding}</charset>
			<pattern>${logPattern}</pattern>
		</encoder>
	</appender>

	<!-- Tu código: INFO para ver login, errores de negocio, etc. -->
	<logger name="py.com.jaimeferreira.ccr" level="INFO" additivity="false">
		<appender-ref ref="CONSOLE" />
		<appender-ref ref="fileAppender"/>
	</logger>

	<!-- Spring: solo WARN (errores y advertencias, sin ruido) -->
	<logger name="org.springframework" level="WARN" additivity="false">
		<appender-ref ref="CONSOLE" />
		<appender-ref ref="fileAppender"/>
	</logger>

	<!-- Hibernate SQL: silenciado en prod -->
	<logger name="org.hibernate" level="WARN" additivity="false">
		<appender-ref ref="CONSOLE" />
		<appender-ref ref="fileAppender"/>
	</logger>

	<!-- Root: solo ERROR para todo lo demás -->
	<root level="ERROR">
		<appender-ref ref="CONSOLE"/>
		<appender-ref ref="fileAppender"/>
	</root>

</configuration>